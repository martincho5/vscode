get-content D:\scripts\computers.txt | Get-AdComputer -ea 0 | Move-AdObject -TargetPath 'OU=SinProxy,OU=Empleados, DC=uade, DC=edu, DC=ar'


$users = get-content D:\scripts\usuariosdisis.txt


ForEach ($user in $users) { 
    $UserDN  = (Get-ADUser -Identity $user).distinguishedName
Move-ADObject  -Identity $UserDN  -TargetPath $ou}


Install-Package Docker -ProviderName DockerProvider -Force